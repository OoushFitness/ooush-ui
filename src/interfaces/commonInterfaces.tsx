export default interface OoushTableRow {
    name: string,
    id: number | null,
    weight: number,
    reps: number
}

export default interface OoushExerciseTableRow {
    id: number | null,
    name: string
}